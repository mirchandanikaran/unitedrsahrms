[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
# Avoid broken third-party pytest plugins from the global environment (optional)
filterwarnings =
    ignore::DeprecationWarning:httpx
    ignore::DeprecationWarning:starlette
